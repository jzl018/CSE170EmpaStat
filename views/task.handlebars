<!DOCTYPE html>
<html>
  <head>
    <title>Multus</title> 
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Karma">

    <link href="/css/bootstrap.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.css" rel="stylesheet">
    <link href="/css/multus.css" rel="stylesheet"/>
    <link href="/css/w3.css" rel="stylesheet"/>
  </head>
<style>
body,h1,h2,h3,h4,h5,h6 {font-family: "Karma", sans-serif}
.w3-sidenav a {padding:20px}
</style>
<body class="bg-color">
<!-- Top menu -->
<!-- <form id="addTaskForm" role="form" method="get" action="/modify"> -->
<form id="addTaskForm" role="form" method="get" > <!-- onsubmit="return setProgress()"> -->
  <div class="w3-top">
    <div class="w3-xlarge w3-padding-xlarge" style="max-width:1200px;margin:auto">
      <div class="w3-opennav w3-left w3-hover-text-grey" onclick="w3_open()">
        <h3><a href="/category/{{cid}}" style="text-decoration:none;" id="thisdate">X</a></h3>
      </div>
      <div class="w3-center" id="text-design">
       &nbsp &nbsp &nbsp
         <input type="text" name="tname" id="tname" style="text-align:center;" value="{{tname}}" required>
        <!-- <h2>{{name}}</h2> -->
      </div>
    </div>
  </div> <br/>
  <!-- !PAGE CONTENT! -->
  <div class="w3-main w3-content w3-padding" id="text-design"style="max-width:1200px;margin-top:100px">
    <div class="w3-center">
      <br/> <br/>
      Complete <input type="checkbox" name="complete" id="complete" value="yes">
      <input type="number" name="tid" value="{{tid}}" hidden>
      <h4>Difficulty</h4>
      <input type="range" name="difficulty" min="1" max="5" value="{{difficulty}}">
      <h4>Duration</h4>
      <input type="number" name="duration" min="1" max="10" value="{{duration}}"> hour(s)
      <h4>Reminders</h4>
      &nbsp &nbsp &nbsp &nbsp Start By:
      <input type="datetime-local" name="startdate" value="{{startdate}}"> <br/> <br/>
      Remind <input type="number" name="startreminder" min="0" step="10" max="180" value="{{startreminder}}"> minutes before start <br/> <br/>
      Complete By:
      <input type="datetime-local" name="enddate" value="{{enddate}}"> <br/> <br/>
      Remind <input type="number" name="endreminder" min="0" step="10" max="180" value="{{endreminder}}"> minutes before due <br/> <br/>
      <input type="number" name="progress" id="progress" min="0" max="100" value="0" hidden>
      
      <input type="submit" id="delete-button" name="delete" formaction="/category/{{cid}}/remove" value="Delete" class="btn w3-button" onclick="return confirm('Are you sure you want to delete this task?');">

      <button type="submit" id="save-button" name="modify" formaction="/category/{{cid}}/modify"  class="btn w3-button">Save</button>
      <input type="text" name="cname" id="cname" value="{{category}}" hidden>
      <input type="text" name="cid" id="cid" value="{{cid}}" hidden>
    </div>
  </div>


  <!-- End page content -->
  </div>

  <script>
    // Script to open and close sidenav
    function w3_open() {
        document.getElementById("mySidenav").style.display = "block";
    }
     
    function w3_close() {
        document.getElementById("mySidenav").style.display = "none";
    }
  </script>

  <script type="text/javascript">
    function setProgress() {
      console.log("ASDFSAFDSAF");

      /*
      // Set progress from subtasks
      var prog = 0;
      if (document.getElementById("subtask1").value.replace(/ /g,'') != ''
         && document.getElementById("subtask1status").checked == true) {
        prog += 33;
      }
      if (document.getElementById("subtask2").value.replace(/ /g,'') != ''
         && document.getElementById("subtask2status").checked == true) {
        prog += 33;
      }
      if (document.getElementById("subtask3").value.replace(/ /g,'') != ''
         && document.getElementById("subtask3status").checked == true) {
        prog += 33;
      }
      if (prog === 99) {
        prog = 100;
      }
      document.getElementById("progress").value = prog;

      // Popup confirmation
      if (prog === 100) {
        if (confirm("You have marked all subtasks as completed. This task will accordingly be move to your 'History' tab.") == false) {
          return false;
        }
        else {
          document.getElementById("complete").value = "yes";
        }
      }
      */

      /*
      alert(document.getElementById("complete").checked);
      if (document.getElementById("complete").checked) {
        if (confirm("You have marked this task as complete. This task will accordingly be move to your 'History' tab.") == false) {
            return false;
        }
      }
      */

      // Return to home menu
      window.location.href = '/category/{{cid}}';
    }
  </script>

    <!-- Google Analytics -->
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-92553441-2', 'auto');
  ga('send', 'pageview');

</script>
  </body>
</html>
